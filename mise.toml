[tasks.freshness]
description = "Check pinned tool and GitHub Action versions for drift (pass '-- --apply' to patch)"
run = "doneram check"

[tools]
actionlint = "1.7.12"
# For running pre-commit with calcipy
"pipx:calcipy" = {extras = "recommended", version = "6.0.1"}
