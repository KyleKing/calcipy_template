[tasks.freshness]
description = "Check pinned GitHub Action versions in package_template for drift (pass '-- --apply' to patch)"
run = "uv run python scripts/check_freshness.py"

[tools]
# For running pre-commit with calcipy
"pipx:calcipy" = {extras = "recommended", version = "6.0.1"}
